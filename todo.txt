PHASE 0 — Environment Setup

Goal: Clean development environment.

Tasks

1️⃣ Create project folder

smart_attendance

2️⃣ Create virtual environment

python -m venv venv

3️⃣ Install dependencies

pip install opencv-python
pip install mediapipe
pip install insightface
pip install numpy
pip install fastapi
pip install uvicorn
pip install sqlalchemy
pip install pydantic

4️⃣ Verify webcam index

Test:

cv2.VideoCapture(2)

Output should show webcam feed.

PHASE 1 — Camera Streaming

Goal: Stable real-time webcam feed.

Tasks

Build:

camera/camera_stream.py

Features:

read webcam index 2

show live frames

quit with q

Success condition:

Stable 30 FPS camera feed
PHASE 2 — Face Detection

Goal: Detect faces with BlazeFace.

Tasks

Create:

vision/detector.py

Responsibilities:

input → frame
output → face bounding boxes

Test pipeline:

camera frame → detect faces → draw boxes

Success condition:

Face boxes appear on screen
PHASE 3 — Face Cropping + Alignment

Goal: Extract clean face images.

Tasks:

Create:

vision/aligner.py

Process:

frame
   ↓
face bbox
   ↓
crop face
   ↓
resize to 112x112

Success condition:

face crop displayed correctly
PHASE 4 — ArcFace Embedding

Goal: Convert face image → 512 vector.

Create:

vision/embedder.py

Pipeline:

face image
   ↓
ArcFace
   ↓
512 embedding

Success condition:

print(len(embedding)) == 512
PHASE 5 — Face Matching

Goal: Identify known users.

Create:

recognition/matcher.py

Function:

match_embedding(new_embedding)

Algorithm:

for each stored embedding
     compute cosine similarity
     pick best match

Threshold:

0.55

Success condition:

Same person → matched
Different person → rejected
PHASE 6 — Database System

Goal: Store users and embeddings.

Create:

database/db.py
database/models.py

Tables:

USERS
id
name
created_at
EMBEDDINGS
id
user_id
embedding_vector
ATTENDANCE
id
user_id
date
time
status

Success condition:

User + embedding stored
PHASE 7 — Registration System

Goal: Register new student faces.

Workflow:

Enter name
Stand in camera
Capture 5 frames
Generate embeddings
Average embeddings
Store in database

File:

attendance/register_user.py

Success condition:

User registered
PHASE 8 — Real-Time Recognition

Goal: Automatic recognition.

Pipeline:

camera
   ↓
detect
   ↓
embedding
   ↓
match
   ↓
identify user

Output example:

Detected: Vedant
Similarity: 0.71
PHASE 9 — Attendance Logic

Goal: Mark attendance once per day.

Create:

attendance/attendance_service.py

Logic:

if attendance already exists today:
     reject
else:
     mark attendance

Success condition:

Duplicate prevention works
PHASE 10 — FastAPI Backend

Goal: API control.

Endpoints:

POST /register
GET /attendance
GET /users

Allows future GUI integration.

PHASE 11 — Dashboard (Optional)

Possible options:

Streamlit
React dashboard
PyQt

Features:

attendance table
user list
statistics
PHASE 12 — Raspberry Pi Optimization

Later tasks:

model quantization
frame skipping
tracking instead of detection

Goal:

≥10 FPS on Raspberry Pi
Timeline Estimate

If focused:

Phase	Time
0–2	1 day
3–4	1 day
5–7	2 days
8–10	2 days

Total:

~6 days